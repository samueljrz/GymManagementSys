/*
const name = "Samuel"
const weight = 65
const height = 1.65

const imc = weight / (height*height)

if(imc >= 30) {
    console.log(`${name} you are above of the weight`)
}else {
    console.log(`${name} you aren't above of the weight`)
}
*/
/*
const name = "Samuel"
const gender = "M"
const age = 21
const contribuition = 0

if(gender == "M") {
    if(contribuition >= 35 && (contribuition+age) >= 95) {
        console.log(`${name} you can retire`)
    }else {
        console.log(`${name} you cannot retire`)
    }
}else {
    if(contribuition >= 30 && (contribuition+age) >= 85) {
        console.log(`${name} you can retire`)
    }else {
        console.log(`${name} you cannot retire`)
    }
}
*/